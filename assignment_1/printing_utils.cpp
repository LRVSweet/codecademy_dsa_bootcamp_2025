#include <bits/stdc++.h>

#include "printing_utils.hpp"

using namespace std;

void print_char (int count, char char_to_print)
{
    for (int i = 0; i < count; i++)
    {
        cout << char_to_print;
    }
}