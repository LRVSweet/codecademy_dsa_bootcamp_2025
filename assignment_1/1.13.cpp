#include <bits/stdc++.h>

using namespace std;

int main ()
{
    cout << "Please enter a number." << endl;
    int input;
    cin >> input;

    int size = 2 * input - 1;

    for (int row = 0; row < size; row++)
    {
        for (int col = 0; col < size; col++)
        {
        
        }
    }
}